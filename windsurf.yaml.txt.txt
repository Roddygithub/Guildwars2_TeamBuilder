project:
  name: gw2-team-builder
  description: Logiciel intelligent de création d'équipes optimisées pour Guild Wars 2, basé sur les synergies entre professions, builds, rôles et mécaniques de jeu.
  type: app  # Laisse Windsurf choisir entre web, desktop, mobile ou hybride

backend:
  language: python
  entrypoint: app/main.py
  dependencies:
    - requests
    - fastapi
    - uvicorn
    - pydantic
    - sqlalchemy

frontend:
  auto: true  # Délégation totale à Windsurf pour le choix de l’UI
  description: Interface interactive permettant de composer des équipes, visualiser les synergies, et explorer les builds de GW2.

tasks:
  update-data:
    schedule: "0 4 * * *"
    command: "python app/tasks/update_data.py"
    description: "Mise à jour automatique des données de Guild Wars 2 via l'API officielle"

plugins:
  - github-actions
  - docker
  - auto-ui  # Pour que Windsurf gère totalement l’UI
